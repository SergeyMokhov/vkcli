package auth

import (
	"net/url"
)

func NewAuthUrl(clientId string, redirectUri url.URL) (authUrl url.URL) {
	//todo Finish the method. Maybe move stuff to auth package
	//authUrl := url.Parse(vk.Endpoint.AuthURL)
	return
}
